apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-agent-claim
spec:
  storageClassName: do-block-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
